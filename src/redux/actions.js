// !!! ЦЕ СТАРИЙ ФАЙЛ !!!
// всі екшени завдань були перенесені в src/redux/tasksSlice.js
// екшен фільтра перенесен в src/redux/filtersSlice.js

/*
// 1. Імпортуємо функцію createAction
import { createAction } from "@reduxjs/toolkit";

// 2. Створюємо фабрики екшенів
export const addTask = createAction("tasks/addTask");

export const deleteTask = createAction("tasks/deleteTask");

export const toggleCompleted = createAction(
  "tasks/toggleCompleted",
);

export const setStatusFilter = createAction(
  "filters/setStatusFilter",
);

console.log(toggleCompleted(1));
// { type: "tasks/toggleCompleted", payload: 1 }

console.log(setStatusFilter("hello"));
// { type: "filters/setStatusFilter", payload: "hello" }
*/
